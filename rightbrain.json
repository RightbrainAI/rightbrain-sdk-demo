{
  "$schema": "https://app.rightbrain.ai/cli/schema.json",
  "orgId": "${RB_ORG_ID}",
  "projectId": "${RB_PROJECT_ID}",
  "apiKey": "${RB_API_KEY}",
  "taskIds": [
    /* Profile Image Verification */ "${NEXT_PUBLIC_PROFILE_IMAGE_VERIFICATION_TASK_ID}",
    /* Generate Image-Based Product Listing */ "${NEXT_PUBLIC_GENERATE_IMAGE_BASED_PRODUCT_LISTING_TASK_ID}",
    /* PRD Analysis */ "${NEXT_PUBLIC_PRD_ANALYSIS_TASK_ID}"
  ]
}
